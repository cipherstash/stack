{
  "name": "@cipherstash/ffi-integration-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Live integration coverage for @cipherstash/protect-ffi — real ZeroKMS, real Postgres, EQL v2 and v3",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc",
    "vitest:live": "vitest run",
    "vitest:live:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@cipherstash/auth": "catalog:repo",
    "@cipherstash/protect-ffi": "workspace:*",
    "pg": "8.23.0"
  },
  "devDependencies": {
    "@cipherstash/eql": "workspace:^",
    "@types/pg": "^8.23.1",
    "@vitest/coverage-v8": "catalog:repo",
    "dotenv": "17.4.2",
    "typescript": "catalog:repo",
    "vitest": "catalog:repo"
  }
}
