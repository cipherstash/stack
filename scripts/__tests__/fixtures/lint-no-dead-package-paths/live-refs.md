# Live references

- `packages/stack/src/encryption/index.ts` is the client.
- `packages/cli` owns the `stash` binary; see `./packages/cli/AGENTS.md`.
- `packages/stack-drizzle` and `packages/stack-supabase` are the split adapters.
